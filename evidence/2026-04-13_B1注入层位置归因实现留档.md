# 2026-04-13 B1 注入层位置归因实现留档

日期：2026-04-13
最后验证日期：2026-04-13
状态：已实现，待本地重跑

## 本次实现

1. 在 `scripts/v4_eval_qwen3.py` 新增：
   - `--stage1_inject_layers`
   - `--stage2_inject_layers`
   - `--stage3_inject_layers`
2. 新增 `scripts/audit/run_layer_position_audit.py`：
   - 默认对比 `early_4_11 / mid_8_15 / late_12_19`；
   - 自动写出 `manifest.json`；
   - 便于在本地 GPU 环境直接批量跑三组结果。
3. 新增设计文档：
   - `docs/plans/2026-04-13-B1注入层位置归因实验设计.md`
4. 更新任务清单：
   - `docs/P0实施任务清单.md`

## 当前判断

- gate 已基本完成首轮排查，当前优先级已明确切换到 B1；
- 本次实现的目标是先做层位归因，而不是立即动训练或 loss；
- 下一步依赖用户在 `persona` 环境用 GPU 重跑三组对照实验。

## 下一步命令

推荐直接运行：

```bash
python scripts/audit/run_layer_position_audit.py \
  --device cuda:0 \
  --judge_model GPT-5.2 \
  --stage stage3 \
  --checkpoint checkpoints/stage3_qwen3_v2/best.pt \
  --num_samples 30 \
  --max_turns_per_sample 3
```

若只想先打印命令：

```bash
python scripts/audit/run_layer_position_audit.py --print_only
```


## 补充：首轮空跑排查

- 首轮 `results/layer_position_audit/stage3_20260413_104248/` 未产出有效评估结果。
- 原因不是模型行为，而是默认 checkpoint 误写为 `checkpoints/stage3_qwen3_v2/best.pt`。
- 仓库内真实存在且与当前权威评估一致的路径为 `checkpoints/stage3_qwen3_v2/best.pt`。
- 已修正 `scripts/v4_eval_qwen3.py` 与 `scripts/audit/run_layer_position_audit.py` 的默认值。
- 因此，这一轮 B1 结果应判定为“空跑无效”，不能用于层位归因结论。
