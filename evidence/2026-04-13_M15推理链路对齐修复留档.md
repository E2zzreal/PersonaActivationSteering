# 2026-04-13 M1.5 推理链路对齐修复留档

日期：2026-04-13
最后验证日期：2026-04-13
状态：已实现

## 修改内容

- 将 `scripts/v4_eval_qwen3.py` 中 injected 生成入口改为：
  - 使用 Qwen3 chat template；
  - 使用与 baseline 一致的 `/no_think` system 指令；
  - 显式 `enable_thinking=False`；
  - 只解码新增 token；
  - 保持 `temperature=0.7` 与 `top_p=0.9`。

## 目的

- 缩小 baseline 与 injected 的推理协议差异；
- 优先排除“输入链路偏差放大泄露”的解释；
- 为后续继续判断 injected 高泄露根因提供更可信口径。
