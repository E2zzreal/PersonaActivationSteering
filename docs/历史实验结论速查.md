# PersonaSteer 历史实验结论速查

> 本文提炼截至 2026-04-20 的历史实验关键结论，原始文档已归档至 `docs/archive/`。

---

## A. 推理链路对齐（M1.5，2026-04-13）

**结论**：统一推理链路后，旧评估结论需修正。

| 配置 | 统一链路均分 | 泄露率 |
|------|---:|---:|
| stage2 | **3.589** | 2.22% |
| baseline | 3.556 | 1.11% |
| stage1 | 3.533 | 0.00% |
| stage3 | 3.533 | 2.22% |

- 旧口径（未对齐）下观察到的 20-30% 泄露是**推理链路不一致造成的偏差**，不是注入机制本身的问题
- 对齐后 injected 模型不再有系统性高泄露；stage2 微弱领先 baseline
- 修复内容：injected 统一使用 chat template + `/no_think` + `enable_thinking=False`

---

## B. 注入层位置归因（B1，2026-04-13）

**结论**：`[8..15]` 不是好默认值，`[4..11]` 更优。

| 窗口 | 评分趋势 |
|------|------|
| `early_4_11`（前移） | 最优候选 |
| `mid_8_15`（当前默认） | 不推荐继续使用 |
| `late_16_23`（后层） | 最差 |

- 问题未被根治：层位置是影响因素之一，但不是唯一根因
- **后续实验默认注入层应切换为 `[4..11]`**

---

## C. 训练数据质量（2026-04-13）

- 训练集显式伪思考污染极低（非主因）
- 主要根因是：训练监督目标（ALOE gold）的人格对齐信号太弱
- Gate 接近饱和，区分能力有限

---

## D. 三源评估（2026-04-17，权威结论）

见 `docs/三源人格对齐能力综合分析报告_2026-04-17.md`

| 来源 | GPT-5.4 均分 | 集成均分 |
|------|---:|---:|
| Claude-Opus-4.6 | **3.833** | 3.867 |
| Qwen3-4B baseline | 2.833 | 2.767 |
| ALOE gold | 2.600 | 2.367 |

**根本结论**：ALOE 数据上限 < Qwen3 自然输出水平，是注入增益有限的根因。

---

## E. 归因优先级（D > E > L > I，2026-04-16）

| 层 | 问题 | 状态 |
|---|---|---|
| **D（数据）** | ALOE 上限 2.6 < baseline 2.83 | 已验证（Claude SFT +0.167） |
| **E（评估）** | 弱 rubric 系统虚高 0.6 分 | 已修复（v3 严格 rubric） |
| **L（损失）** | SCL 三层 bug 导致从未生效 | 已修复但效果有限（见 G 节） |
| **I（注入）** | gate 参数硬编码 / 层位置 | 已修复（P0-1）/ 待验证（B1） |

---

## F. D 层重建实验（2026-04-19，D4 评估）

**实验**：661 条 Claude 多轮对话替换 ALOE 作为 Stage1 SFT 训练数据（inject_layers=[4..11]）

| 来源 | GPT-5.4 均分 | 对比 baseline |
|------|---:|---:|
| Claude-Opus-4.6 | 3.833 | +1.000 |
| **新模型（Claude SFT Stage1）** | **3.000** | **+0.167 ✅** |
| Qwen3-4B baseline | 2.833 | 基准 |
| ALOE gold | 2.600 | -0.233 |

**分数分布**：0×1分, 1×2分, 28×3分, 1×4分（几乎全部 3 分）

**结论**：
- 假设成立：Claude 数据确实有效，突破 baseline +0.167（+6%）
- "3 分墙"：模型学到了更好的对话，但缺乏人格不可替换的行为签名
- Judge 评语：「温暖友好自然，但换成任何外向型人格也成立」
- 2 分案例：出现身份混淆（模型编造了不属于人格的个人经历）

**下一步**：DPO 阶段（preferred=Claude, rejected=Qwen3，已有 3305 条偏好对）有望推动 3→4 分突破

---

## G. 三阶段完整管道实验（2026-04-20，661 条 Claude SFT 数据）

**管道**：Stage1(SFT) → Stage2(SFT+gate) → Stage3(SFT+gate+SCL)，inject_layers=[4..11]

| 模型 | 均分 | 分布 | 说明 |
|------|---:|------|------|
| Claude | 3.833 | — | 教师上限 |
| **Stage2** | **3.033** | 1×2, 27×3, 2×4 | **最佳配置** |
| Stage1 | 3.000 | 1×2, 28×3, 1×4 | SFT only |
| Stage3 (scl=0.1) | 3.000 | 30×3 | SCL 贡献太小 |
| Stage3 (scl=1.0) | 2.967 | 1×2, 29×3 | SCL 反而有害 |
| baseline | 2.833 | — | Qwen3-4B 无注入 |

### SCL 三层 bug 修复历程

| Bug | 原因 | 修复 |
|-----|------|------|
| batch_size=1 → SCL=0 | 单样本无对比 | batch_size=4 + grouped sampler |
| user_ids 唯一 → pos_mask=0 | trainer 传 user_ids，SCL 优先用 user_ids 匹配 | 改为用 personality 字符串匹配 |
| 全部同 personality → 无负例 | grouped sampler 整 batch 同 personality | 混合 batch：2 personality × 2 samples |

三层 bug 全部修复后，SCL 仍然无效（Δ=0.003/6 epochs），根因：

### v_t 不分化的根本原因

- 冻结 encoder（Qwen3-4B backbone）对不同 personality 文本的 embedding 差异极小
- projection MLP 无法放大微小的 embedding 差异
- gate_init_bias=-2.0（注入强度 12%）进一步压缩 v_t 对输出的影响
- 结果：所有 personality 的 v_t 几乎相同，SCL 无梯度可优化

### DPO 同样失效

DPO 依赖 token 级 logprob 差异，但 12% 注入强度导致 80% 步骤 margin=0（模型和参考模型 logprobs 相同）。

### 突破方向（等全量 2777 条数据）

1. 增大 gate_init_bias（-2.0 → -0.5，注入强度 12% → 38%）
2. 数据量 ×4（661 → 2777），减少过拟合
3. 或重新设计 HyperNetwork 使其更好地区分 personality

---

## H. 瓶颈诊断实验（2026-04-20）

**目的**：确定 3 分墙的根因是 encoder 相似度还是 gate 注入强度。

**预诊断**：14 个 personality 的 encoder embedding 余弦相似度 **均值 0.959**（几乎无区分度）。

| 实验 | 改动 | 均分 | gate_entropy |
|------|------|---:|---:|
| A1 (Embedding Table) | 14 个可学习向量替换 encoder | 3.000 | **-1.81** |
| B (gate=0.0) | 注入强度 12% → 50% | 3.000 | -2.08 |
| Stage2（对照） | 原始配置 | 3.033 | -2.07 |

**关键发现**：

- ❌ **encoder 不是瓶颈**：A1 用完美分化的 embedding + 更活跃的 gate，仍然 3.0
- ❌ **gate 强度不是瓶颈**：B 用 50% 注入，仍然 3.0
- ✅ **注入机制本身工作正常**（gate_entropy 在 A1 中显著不同，证明 gate 能响应不同输入）

**结论**：3 分墙的根因不在注入机制（I 层），而在数据质量（D 层）：
- Claude 训练数据中的人格签名密度不足（Claude 自身 30 条也大量 3 分）
- SFT 只教「怎么说」不教「为什么这个 persona 必须这么说」
- 661 条 × 14 personality ≈ 47 条/personality，样本量不足以学到精细人格差异

**后续方向**：
1. 升级 Claude 生成 prompt（加入行为约束，提高训练数据的人格签名密度）
2. 等全量 2777 条数据 + 升级后的 prompt 重新生成
3. 探索对比训练目标（同一用户输入，不同 persona 回复的 pair-wise 对比）

---

## I. Prompt 升级试验 + 跨 persona 对比数据设计（2026-04-20）

### Prompt 升级试验

对比旧 prompt（「请以该人格说话」）vs 新 prompt（行为约束：2+ 不可替换标记 / show-don't-tell / 主线特征聚焦）

| Prompt | Claude 均分 (10 条) | 分布 |
|--------|---:|------|
| 旧（简单指令） | 4.00 | 1×3, 8×4, 1×5 |
| 新（行为约束） | 4.00 | 1×3, 9×4 |

**结论**：Claude 用简单指令已能生成 4.0 分回复。Prompt 升级对 Claude 无显著提升。
**推论**：3 分墙的瓶颈不在 Claude 生成端（上游 4.0），而在 SFT 训练的「知识蒸馏损失」——SFT 将所有 persona 回复向同一个「Claude 风格」拟合，磨平 persona 差异。

### 跨 persona 对比数据

**核心洞察**：当前训练数据的每条用户输入只对应 1 个 persona 回复。模型可以仅靠 user_input 预测回复，完全忽略 persona 信号。

**解决方案**：同一 user_input × 多个不同 persona → 不同回复。模型被迫使用 persona 信号区分输出。

| 数据类型 | 训练信号 |
|---------|---------|
| 原始 SFT | (persona_A, input_1) → resp_1, (persona_B, input_2) → resp_2（可忽略 persona）|
| **跨 persona** | (persona_A, input_1) → resp_A, (persona_B, input_1) → resp_B（必须用 persona）|

**状态**：小批量生成中（14/90 条），使用 `scripts/audit/generate_cross_persona.py`。

---

## 项目整体进度总结（2026-04-20）

### 已完成里程碑

| 阶段 | 内容 | 结果 |
|------|------|------|
| 三源评估 | Claude / Qwen3 / ALOE 并行评估 | ALOE 2.6 < baseline 2.83 < Claude 3.83 |
| P0 修复 | gate 传递 + SCL 三层 bug | 全部修复，注入机制工作正常 |
| D 层重建 | 661 条 Claude SFT 数据 | Stage2 最高 3.033（突破 baseline） |
| 三阶段管道 | Stage1/2/3 完整训练 + 评估 | Stage2 最佳，SCL 无效 |
| 瓶颈诊断 | A1(Emb Table) / B(gate=0.0) | 注入机制不是瓶颈，数据+目标才是 |
| Prompt 试验 | 行为约束 prompt 升级 | Claude 已 4.0，prompt 不是瓶颈 |

### 进行中

| 任务 | 进度 | 预计完成 |
|------|------|---------|
| train.jsonl 全量生成 | 1315/2777 (47%) | ~2 天 |
| 跨 persona 对比数据 | 14/90 | ~2 小时 |

### 核心结论链

```
ALOE 数据太弱（2.6） → 换 Claude 数据（4.0）
  → SFT 训练后只得 3.0（损失 1 分）
    → 不是注入机制问题（A1/B 诊断排除）
    → 不是 Claude 质量问题（prompt 试验排除）
    → 是 SFT 目标问题：无 persona 对比信号，所有 persona 向同一风格拟合
      → 解决方案：跨 persona 对比训练数据（进行中）
```

---

## J. Big Five 人格理论整合可行性分析（2026-04-20）

### 动机

14 个 ALOE personality 的 encoder embedding 余弦相似度 0.959，怀疑 personality 描述本身的区分度不足。

### Big Five 标注结果

用 GPT-5.4 对 14 个 ALOE personality 自动标注 [O,C,E,A,N] 分数（-1 到 1）：
- 所有 personality 都是「正面人格」：高 O (0.45~0.90)、高 C (0.10~0.82)、高 A (0.55~0.90)、低 N (-0.05~-0.60)
- 5D 向量余弦相似度均值 0.932（仍然很高，因为分布集中在同一象限）

### 三种整合方案测试

| 方案 | encoder 相似度 | 结论 |
|------|---:|------|
| 原始自由文本 | 0.959 | 基线 |
| 方案 3: 结构化 Big Five 文本 | **0.975** | ❌ 更差（模板格式增加相似度）|
| 方案 2: 5D 数字分支 | 不过 encoder | ✅ 唯一可行路线 |

### 结论

1. 方案 3 被排除：结构化文本模板反而增加 encoder 相似度
2. 方案 2（5D 分支）是正确方向：在 encoder 输出旁加 `Linear(5, v_dim)` 的 Big Five 分支
3. 但 ALOE personality 自身区分度不足：14 个全聚集在 Big Five 正面象限，需要设计更广覆盖的 personality 配置

---

## K. Big Five 突破实验（2026-04-20）

### 实验配置

- 数据：199 条跨 persona 对比数据（16 Big Five personality × 45 用户输入 × 4 persona/输入）
- 注入层：[16..23]（Probing 推荐，R²=0.559）
- gate_init_bias：-1.0（26% 注入强度）
- HyperNetwork：新增 5D Big Five 分支（big5_projector + big5_gate）

### 结果

| 模型 | 均分 | 对比 baseline |
|------|---:|---:|
| **Big Five Stage1** | **3.75** | **+0.92** |
| ALOE Stage2（之前最佳） | 3.033 | +0.20 |
| baseline | 2.833 | — |

### 分数分布（6 条快速评估）

| Persona | 分数 | 回复特征 |
|---------|---:|---------|
| Stoic | 4 | 极简务实，"maintaining systems and routines" |
| Caretaker | 4 | 焦虑关怀，"feeling a bit down... not sure if doing right things" |
| Rebel | 4 | 反主流，"hacking into the city's data grid" |
| Scholar | 3 | 精确但破功（说自己是 AI） |

### 突破原因（三因素叠加）

1. **Big Five 结构化 personality**：5D 余弦均值 0.037（vs ALOE 0.961），输入区分度提升 25 倍
2. **Probing 推荐注入层 [16..23]**：人格信息密度最高的层（R²=0.559）
3. **跨 persona 对比数据**：同一输入 × 不同 persona → 模型必须用 persona 信号区分输出

### Probing 发现

36 层线性探针分析，各 Big Five 维度在不同深度的信息密度：

| 维度 | 最强层 | 峰值 R² | 模式 |
|------|---:|---:|------|
| C (尽责性) | 19 | 0.815 | 中深层，最强维度 |
| A (宜人性) | 7-21 | 0.711 | 中间层稳定带 |
| E (外向性) | 35 | 0.685 | 逐层递增，深层最强 |
| O (开放性) | 8-10 | 0.386 | 较浅层，最弱维度 |
| N (神经质) | 29 | 0.396 | 全程较弱 |

### 后续方向

1. 扩大 Big Five 数据量（199 → 1000+ 条）
2. 注意力头级别 probing（找到人格专用 attention head）
3. Stage2/3 管道（SCL 在 Big Five 空间应能真正生效）
4. 30 条完整评估（当前仅 6 条快速评估）

---

## L. 公平对比：PersonaSteer vs Baseline（2026-04-21，关键发现）

### 实验设计

控制所有变量，仅改变「是否使用 PersonaSteer 注入」：

| 变量 | 设置 |
|------|------|
| Personality | Big Five 16 个（同一组） |
| 用户输入 | 50 条（随机抽样） |
| Judge | GPT-5.4，严格 v3 rubric |
| System prompt | 相同（包含 personality 描述） |

### 结果

| 模型 | 均分 | 分布 |
|------|---:|------|
| Big Five PersonaSteer | 3.503 | 4×2, 111×3, 61×4, 21×5 |
| **Qwen3-4B Baseline（仅 prompt）** | **3.500** | 6×2, 15×3, 27×4, 2×5 |
| 差异 | **+0.003** | **无统计显著性** |

### 结论

**PersonaSteer 注入机制在当前设置下没有提供额外增益。**

- 3.033→3.503 的提升完全来自更好的 personality 描述（Big Five + behavioral markers）
- v_t 干预向量在 personality 已在 prompt 中时是冗余的
- Baseline 仅靠 system prompt 就能达到相同水平

### 注入机制可能有价值的场景

1. **无 prompt personality**：用户只提供 5D 向量，不写文字描述
2. **多轮一致性**：v_t 跨轮次维持，prompt 在长对话中可能被 attention 稀释
3. **推理效率**：v_t 不占 context window（对长对话有意义）
4. **可控性**：连续调节 5D 分数，平滑切换人格（prompt 做不到）

### 项目整体结论链（更新）

```
ALOE 数据太弱（2.6）
→ 换 Claude 数据（4.0）→ PersonaSteer 3.0（SFT 磨平差异）
  → 不是注入机制问题（A1/B 诊断）
  → 不是 Claude 质量问题（prompt 试验）
  → 是 personality 描述问题（encoder 0.961）
    → Big Five 结构化 personality → 3.503
      → 但 baseline 不注入也 3.500
      → 结论：提升来自 personality 描述质量，非注入机制
```
