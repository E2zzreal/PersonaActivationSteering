# API配置文件
# 用于LLM Judge评估

# BLSC API配置
blsc:
  api_key: "sk-qF4m66e6_2_VnygJGmxBJQ"  # 请替换为您的实际API key
  base_url: "https://llmapi.blsc.cn"
  model: "GPT-5.2"

# OpenAI API配置（备用）
openai:
  api_key: "YOUR_OPENAI_API_KEY_HERE"  # 如果使用OpenAI，请替换
  base_url: null
  model: "gpt-4o"

# 默认使用哪个API
default: "blsc"
