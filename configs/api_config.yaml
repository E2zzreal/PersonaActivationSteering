# API配置文件
# 用于LLM Judge评估

# BLSC API配置
blsc:
  api_key: "sk-qF4m66e6_2_VnygJGmxBJQ"
  base_url: "https://llmapi.blsc.cn"
  model: "GPT-5.2"
  claude_generator_model: "Claude-Opus-4.6"

# OpenAI API配置（备用）
openai:
  api_key: "YOUR_OPENAI_API_KEY_HERE"
  base_url: null
  model: "gpt-4o"

# 默认使用哪个API
default: "blsc"

# Judge 模型组合（多模型评估用）
judge_models:
  - model: "GPT-5.4"
    api: "blsc"
    max_tokens: 300
  - model: "Gemini-3.1-Pro-Preview"
    api: "blsc"
    max_tokens: 2000
